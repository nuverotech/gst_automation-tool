python -m venv venv -- To create a virtual environment
.\venv\Scripts\Activate.ps1 -- To activate a venv
pip install -r requirements.txt -- To install the requirements
npm install -- Install the required frontend packages
git clone link.git
git checkout -b branch_name -- To create and checkout a new branch
git checkout branch_name -- To checkout a branch
git add . -- To add files to the git
git commit -m "" -- To commit files with a message
git push -u origin branch_name -- To push when using a new branch
git push -- To push the changes
git fetch -- To get the updated online changes
git pull -- To get the latest changes locally
cd dir_name -- To go to that directory in the terminal
cd .. -- To go back one directory in the terminal
uvicorn app.main:app --reload -- To start the backend
npm start -- To start the frontend (remember to be in the frontend directory)
python -c "import redis; r = redis.Redis(host='localhost', port=6379, db=0); print('Ping:', r.ping()); print('Set test:', r.set('test', 'value')); print('Get test:', r.get('test'))"